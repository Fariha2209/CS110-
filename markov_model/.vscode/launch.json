{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Markov Model",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}",
        }
    ]
}
